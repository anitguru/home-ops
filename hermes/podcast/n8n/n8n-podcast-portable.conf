[Service]
EnvironmentFile=-/opt/n8n-podcast.env
# Workflow JSON contains only $env references. Secret values remain in the
# machine-specific SOPS materialization and are restored during migration.
Environment=N8N_BLOCK_ENV_ACCESS_IN_NODE=false
